{% extends 'base.html.twig' %}

{% block title %}{{ anime.title }} - Anime Details{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-4">
                <img src="{{ anime.picture }}" class="img-fluid rounded" alt="{{ anime.title }}">
            </div>
            <div class="col-md-8">
                <h1 class="display-4">{{ anime.title }}</h1>
                <p class="text-muted">ID: {{ anime.id }}</p>

                <a href="/anime" class="btn btn-primary mt-3">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}